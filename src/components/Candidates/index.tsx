import { Box } from '@mui/material';
import { modalBG } from 'helpers/styles';

const Candidates = () => (
  <Box sx={{ bgcolor: modalBG, p: '10px', height: '100vh' }}></Box>
);

export default Candidates;
